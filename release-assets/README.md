# Release Assets

This folder stores shareable release packages through Git LFS.

- `Dynamic Island 1.3.0 安装包.zip` contains both the Setup installer and the Portable executable.
- User data is stored outside the install package and is not overwritten by installing a newer version.
